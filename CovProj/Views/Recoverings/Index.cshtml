@model IEnumerable<CovProj.Models.Recovering>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PeoplesId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PeoplesId)
        </td>
        <td>
            @if (Session["Admin"] == "true")
            {@Html.ActionLink("Edit", "Edit", new { id = item.RecoveringId })}|
            @Html.ActionLink("Details", "Details", new { id = item.RecoveringId }) |
            @if (Session["Admin"] == "true") {
                @Html.ActionLink("Delete", "Delete", new { id = item.RecoveringId })}
        </td>
    </tr>
}

</table>
