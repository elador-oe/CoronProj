@model IEnumerable<CovProj.Models.Isolated>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlaceOfIsolation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlaceOfIsolation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IsolatedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.IsolatedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IsolatedId })
        </td>
    </tr>
}

</table>
